
labels:
- includeSelectors: true
  pairs:
    app-name: fortress-frontend

resources:
- deployment.yaml
- service.yaml

configMapGenerator:
- name: fortress-frontend-config
  envs:
  - app.properties

replacements:
- source:
    kind: ConfigMap
    name: fortress-frontend-config
    fieldPath: data.IMAGE
  targets:
  - select:
      kind: Deployment
      name: fortress-frontend
    fieldPaths:
    - spec.template.spec.containers.0.image
