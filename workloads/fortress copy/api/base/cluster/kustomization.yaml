
labels:
- includeSelectors: true
  pairs:
    app-name: fortress-api

resources:
- deployment.yaml
- service.yaml
- network.yaml

configMapGenerator:
- name: app-configmap-1
  envs:
  - app.properties

replacements:
- source:
    kind: ConfigMap
    name: app-configmap-1
    fieldPath: data.IMAGE
  targets:
  - select:
      kind: Deployment
      name: fortress-api
    fieldPaths:
    - spec.template.spec.containers.0.image
